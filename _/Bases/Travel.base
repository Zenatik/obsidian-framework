filters:
  and:
    - '!file.name.endsWith("Template")'
    - categories.containsAny(link("Travel"))
views:
  - type: table
    name: Travel
    order:
      - file.name
      - type
      - start
      - completion
      - locations
    sort:
      - property: start
        direction: DESC
