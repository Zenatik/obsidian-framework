filters:
  and:
    - '!file.name.endsWith("Template")'
    - categories.containsAny(link("Inbox"))
views:
  - type: table
    name: Inbox
    order:
      - file.name
      - created
      - tags
    sort:
      - property: created
        direction: DESC
