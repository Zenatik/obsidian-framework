filters:
  and:
    - '!file.name.endsWith("Template")'
    - categories.containsAny(link("Procedure"))
views:
  - type: table
    name: Procedure
    order:
      - file.name
      - type
    sort:
      - property: locations
        direction: ASC
