filters:
  and:
    - '!file.name.endsWith("Template")'
    - categories.containsAny(link("Learning"))
views:
  - type: table
    name: Learning
    order:
      - file.name
      - type
      - locations
      - completion
