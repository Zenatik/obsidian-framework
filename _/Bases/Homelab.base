filters:
  and:
    - '!file.name.endsWith("Template")'
    - categories.containsAny(link("Homelab"))
views:
  - type: table
    name: Homelab
    order:
      - file.name
      - type
    sort:
      - property: locations
        direction: ASC
