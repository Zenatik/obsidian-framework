filters:
  and:
    - '!file.name.endsWith("Template")'
    - categories.containsAny(link("Companies"))
views:
  - type: table
    name: Companies
    order:
      - file.name
      - type
      - url
